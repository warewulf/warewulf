#!/bin/sh

echo "Hello from WWINIT"

. @WWCLIENTDIR@/config

for i in @WWCLIENTDIR@/init.d/*; do
    NAME=`basename $i`
    echo "Launching: $NAME"
    sh "$i"
done

echo "Calling $WWINIT..."
echo

sleep 2
exec $WWINIT
